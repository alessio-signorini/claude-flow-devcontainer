# Environment variables for Claude Flow development
# Copy this file to .env and customize as needed

# Redis Configuration
REDIS_URL=redis://redis:6379/0

# Rails Configuration
RAILS_ENV=development
SECRET_KEY_BASE=your_secret_key_here

# Claude Flow Configuration (customize these)
# CLAUDE_API_KEY=your_claude_api_key_here
# CLAUDE_FLOW_CONFIG_PATH=/workspace/.claude-flow.yml

# Optional: Additional service URLs
PGADMIN_URL=http://localhost:8080
REDIS_COMMANDER_URL=http://localhost:8081

# Database Configuration (appended by devcontainer setup)
# DATABASE_URL=postgresql://postgres:password@db:5432/postgres_development
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=password
# POSTGRES_DB=postgres

# Database Configuration (from devcontainer)
DATABASE_URL=postgresql://postgres:password@db:5432/postgres_development
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=postgres

# Database Configuration (from devcontainer)
DATABASE_URL=postgresql://postgres:password@db:5432/postgres_development
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=postgres

# Database Configuration (from devcontainer)
DATABASE_URL=postgresql://postgres:password@db:5432/postgres_development
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=postgres

# Database Configuration (from devcontainer)
DATABASE_URL=postgresql://postgres:password@db:5432/postgres_development
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=postgres

# Database Configuration (from devcontainer)
DATABASE_URL=postgresql://postgres:password@db:5432/postgres_development
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=postgres
